# ===================================
# ARTHEA BACKEND - ENVIRONMENT VARIABLES
# ===================================
# Skopiuj ten plik jako .env i wypełnij wartości

# --- SERVER ---
NODE_ENV=development
PORT=5000
API_URL=http://localhost:5000

# --- JWT (generuj losowy secret: openssl rand -base64 32) ---
JWT_SECRET=twoj_super_tajny_klucz_min_32_znaki_abc123xyz
JWT_EXPIRES_IN=7d

# --- BACKBLAZE B2 (S3-compatible) ---
B2_ENDPOINT=https://s3.eu-central-003.backblazeb2.com
B2_REGION=eu-central-003
B2_BUCKET_NAME=twoj-bucket-name
B2_KEY_ID=twoj_key_id_z_backblaze
B2_APP_KEY=twoj_application_key_z_backblaze

# --- EMAIL (opcjonalne, dla notyfikacji) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=twoj-email@gmail.com
SMTP_PASS=twoje_haslo_lub_app_password
EMAIL_FROM=Arthea <noreply@arthea.pl>

# --- DATABASE ---
# SQLite nie wymaga konfiguracji - plik w /data/arthea.db

# --- CORS (frontend URL) ---
FRONTEND_URL=http://localhost:3000